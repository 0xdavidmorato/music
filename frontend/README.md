Frontend scaffold (Svelte + Vite)

This frontend consumes data/ JSON files from the repository root.
During local development, copy the data/ directory into this frontend's public/data or configure a static server.

Run:
  npm install
  npm run dev

